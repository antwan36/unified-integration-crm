# Unified Integration CRM — project status

Read this first if you're picking up work on this project in a new Claude Code session (e.g. on a different Mac). It's a native Electron CRM built for Unified Integration (Lancaster PA smart-home/AV installer), covering leads, email, invoicing, estimates, and scheduling in one place.

**This file changes fast and has fallen out of date before — don't trust it blindly.** Grep the actual source (`src/main/ipc/handlers.ts` for the full IPC surface, `src/shared/types.ts` for data shapes) before assuming a feature is or isn't built. This project has also been observed changing *while* a session is actively reading it (concurrent edits from elsewhere) — if something you read doesn't match what you expected, re-read rather than assume you mis-saw it.

## Architecture
- Electron + React + TypeScript, Vite build (`electron-vite`). Renderer in `src/renderer/src`, main process in `src/main`, shared types in `src/shared/types.ts`.
- **Data storage**: shared **Neon Postgres** database. `src/main/db/` holds the `pg`-based data-access layer. Schema lives in `src/main/db/schema.ts`, applied idempotently on every connect via `ensureSchema()`.
- **Per-machine setup**: each Mac "Connects its workspace" on first launch — paste the same Neon connection string + workspace passphrase used on every other machine (`src/renderer/src/pages/ConnectWorkspace.tsx`, `src/main/secrets/workspace.ts`). The passphrase derives an AES key (fixed salt, `scrypt`) used to encrypt IMAP/Square credentials before they're stored in the shared `settings` table.
- **Auth**: local login gate (`src/main/db/users.ts`), bcrypt-hashed, shared across machines via the same Postgres `users` table. Team accounts: `auth:createUser` lets an already-logged-in user add a teammate (Settings → Team).
- **Email (IMAP + SMTP)**: `src/main/imap/sync.ts` polls INBOX every 10 min (`src/main/backgroundSync.ts`) and on launch, parses Formspree lead-notification emails into new Contacts, links other mail to existing contacts by sender, creates "unmatched" contacts for unknown senders. `src/main/email/smtp.ts` sends via `nodemailer` — compose/reply UI in `ContactDetail.tsx`.
- **Direct Formspree→CRM lead path**: independent of IMAP, every real form on the marketing site (13 as of 2026-07-27, up from 3) fires a parallel fire-and-forget `fetch` straight to `estimate-portal/api/lead.js`, which writes the contact + a `form_submission` activity directly into the same Neon Postgres — so leads land instantly instead of waiting on the next IMAP poll (or on an email account being configured at all). `buildContextLines()` in `lead.js` folds any non-core form field generically into the activity body, mirroring `formspree.ts`'s approach, rather than a hardcoded field-name allowlist. All Formspree submissions (not just ones that get promoted to full leads) are also visible verbatim on the `/form-leads` page (`FormLeads.tsx`) — a flat, searchable log of raw form submissions.
- **Finances** (`/finances`, `Finances.tsx`, three tabs — `finances/BankAccountsTab.tsx`/`PayrollTab.tsx`/`InventoryTab.tsx`): the "rest of the business" beyond leads/invoices.
  - **Bank accounts (Plaid)**: `src/main/plaid/{client,link,sync}.ts` + `src/main/db/plaid.ts`. Settings → "Plaid (bank accounts)" holds a Client ID/Secret (sandbox or production, same encrypted-settings pattern as Square/IMAP — `src/main/secrets/plaid-credentials.ts`). "+ Connect a bank account" opens Plaid Link (`plaid:createLinkToken` → hosted Plaid UI → `plaid:exchangePublicToken`, which stores an encrypted `access_token` per `plaid_items` row and pulls in `bank_accounts`). `plaid:sync` (also run on the 10-min background sync) pulls new `bank_transactions` via Plaid's cursor-based sync. Real bank login happens entirely inside Plaid's own hosted widget — no bank credentials ever pass through this app.
  - **Payroll**: `payees` (contractors/employees, `encryptedTaxId` for 1099 purposes) + `payroll_payments` (what was actually paid out, optionally linked to a `contactId`). 1099 Summary tab totals payments per payee per tax year; tax IDs stay masked until clicked.
  - **Inventory**: `inventory_items` + `inventory_transactions` (in/out ledger) for gear/parts kept in stock.
  - Invoices also gained an internal-only `costCents` (materials/labor cost, never sent to Square or shown to the client — profit-margin view only) and `shippingCents`.
- **Review Requests** (`/review-requests`, `ReviewRequests.tsx`, `src/main/reviewRequests/actions.ts`): a `review_requests` row is auto-queued whenever a Square invoice transitions to PAID (one per invoice, deduped). Nothing sends automatically — each request sits in a review queue until a person clicks send, which emails the contact a review-request message containing the **Google review link** (Settings → "Google review link", grabbed from Business Profile Manager's "Get more reviews" → Copy link — not yet configured as of this session, so Review Requests can be queued/reviewed but not actually sent until that's set).
- **Invoicing (Square)**: `src/main/square/invoices.ts`. Invoices can be sent immediately or saved as a **draft** (`CreateInvoiceInput.draft`) and sent later (`sendDraftInvoice`). Drafts can be deleted; published invoices can be canceled (`deleteOrCancelInvoice`) unless already paid/refunded/canceled/failed. Partial payments and refunds are tracked (`Invoice.paidCents`/`refundedCents`, computed via `sumInvoicePaidCents`/`sumOrderRefundCents` in `square/client.ts`) and refreshed on the same 10-min background sync and on manual refresh.
- **Estimates**: fully custom e-signature flow (Square has no public Estimates API — confirmed via Square's own developer forum). `estimate-portal/` is a separate Vercel project (own repo/folder, not part of this Electron app's build) that serves the signing page (`/e/:token`) and reads/writes directly to the same Neon Postgres via `DATABASE_URL`. Draft estimates can be duplicated into a new draft (`onDuplicate` in `EstimateDetail.tsx`) and lines can be copied into a new invoice too (`CopyFromState`, shared between `NewInvoice.tsx`/`NewEstimate.tsx`).
- **Catalog**: `catalog_items` table — reusable priced line items (name/description/price) pickable from a dropdown (`LineItemsGrid.tsx`, shared by NewInvoice/NewEstimate/EstimateDetail) when building an invoice or estimate. Managed at `/catalog`.
- **Product-link scraping**: paste or drop a product URL into a line item's Link column and `src/main/catalog/scrape.ts` (uses `cheerio`) fetches the page, reads JSON-LD `Product`/`ProductGroup` schema (falling back to OG/meta tags, then a regex `$` price scan) and auto-fills the description/price for that row. Handles bot-blocked sites (403/429/Cloudflare) with a clear error instead of a silent failure.
- **Calendar feed**: `src/main/calendar/feed.ts` generates a `webcal://` link (`Settings` → "Job calendar") pointing at `estimate-portal`'s `/cal/:token.ics` endpoint (`estimate-portal/api/cal/[token].js`), which reads scheduled `tasks` (the ones with `startAt` set) straight from Postgres and serves an RFC 5545 `.ics` feed — subscribe once from Calendar.app/phone and new scheduled jobs show up automatically. The feed URL only appears once the estimate signing page URL (Settings) is saved, since they're served from the same Vercel deployment. **Currently broken in production — see "Known issues" below.**
- **Global search**: Cmd+K opens `SearchPalette.tsx`, a fuzzy-ish substring search across contacts/invoices/estimates loaded client-side.
- **Email section**: `/email` (`src/renderer/src/pages/Email.tsx`) is a unified inbox across all contacts — every `activities` row of `type = 'email'`, joined to its contact, newest first. Backed by `email:list`/`email:unreadCount`/`email:markRead` IPC (`src/main/db/activities.ts`: `listEmailActivities`/`countUnreadEmails`/`markActivityRead`). Read/unread is a plain `read BOOLEAN` column on `activities` (default `TRUE`; IMAP sync explicitly sets `read: false` on newly-synced inbound mail — see `src/main/imap/sync.ts`). Composing to an address with no existing contact auto-creates one via `contacts:findOrCreateByEmail` (`findOrCreateContactByEmail` in `src/main/db/contacts.ts`), marked `unmatched: true` same as unrecognized inbound senders. Sidebar shows an unread-count badge next to "Email" (polled every 60s in `Layout.tsx`).
- **Multiple email accounts**: the app supports any number of IMAP/SMTP accounts (e.g. two company mailboxes), not just one. Managed in Settings (`src/renderer/src/pages/Settings.tsx`, "Email accounts" section) via `emailAccounts:list/create/update/delete/test` IPC, backed by the `email_accounts` table (`src/main/db/emailAccounts.ts` — passwords encrypted per-row with the same workspace-derived key as everything else). `runImapSyncAll()` in `src/main/imap/sync.ts` polls every configured account's INBOX independently (each with its own `sync_state` row, keyed by the account's id) and aggregates results; `backgroundSync.ts` and the "Sync now" button both call it. Every synced/sent email activity carries `emailAccountId` (nullable `TEXT` column on `activities`, no FK) so the Email inbox and each contact's compose/reply UI can show/pick which account a message came from or send through — replying defaults to the account a message arrived on; new compose defaults to the first configured account with a picker if there's more than one. **One-time upgrade path**: this replaced an earlier single-account design (`imap_credentials` under one encrypted `settings` row); `migrateLegacyImapAccountIfNeeded()` runs once at startup/workspace-connect to copy that old account (and its IMAP sync progress) into `email_accounts` if the table is still empty — but as of 2026-07-22 that legacy key was actually never populated on the production workspace (Settings never showed a "Connected" banner for it), so there was nothing to migrate; both real company mailboxes still need to be added fresh via Settings.
- **Revenue chart**: `InvoiceRevenueChart.tsx` on the Invoices page, a small inline SVG bar chart (`InvoiceMonthlyPoint[]` from the invoices IPC).
- **Calendar page**: `/calendar` (`src/renderer/src/pages/Calendar.tsx`) — a month-grid view of the same open Tasks the Tasks list page shows (`tasks.listOpen()`), grouped by effective date (`startAt`'s date, else `dueDate`). Scheduled appointments show with their time in the primary color; plain follow-ups show without a time in gray. Click an event to jump to its contact. New events can be added two ways: the header "+ Add" button (defaults to today), or hovering any day cell reveals a small "+" — both open the same inline form (contact, title, optional time range, optional location) that calls `tasks.create`, same as the Tasks page's own add form. This is a pure frontend view — no new backend/schema needed.
- **In-app self-update**: `src/main/updater.ts`. `checkForUpdate()` hits `https://api.github.com/repos/antwan36/unified-integration-crm/releases/latest` (public repo, no auth needed) and compares `tag_name` to `app.getVersion()`. `UpdateBanner.tsx` (mounted in `Layout.tsx`, above the sidebar+content row) checks on launch and every 4 hours; when a newer release exists it shows "Update available — vX.Y.Z" with an "Update & Restart" button. Clicking it calls `installUpdate(downloadUrl)`, which downloads the matching-architecture `.dmg` asset, writes a detached bash script to a temp dir, spawns it (`detached: true`, `.unref()`), and calls `app.quit()` shortly after. The script waits for the old process to exit, mounts the dmg via `hdiutil`, replaces `/Applications/Unified Integration CRM.app`, strips any quarantine xattr, unmounts, and relaunches — the exact same manual sequence used throughout this project's development, just automated. **No code signing is required for this to work** (replacing a running app's files is fine on macOS/APFS, unlike Windows) — it was verified end-to-end on 2026-07-23 (installed v1.0.1, cut a v1.0.2 GitHub release without touching the running app, confirmed the banner appeared and the full download→quit→swap→relaunch→reopen cycle worked with no Gatekeeper prompt). What's *not* available without a paid Apple Developer cert is a fully silent/zero-click install — this is one-click, not zero-click. **This mechanism is inherently multi-machine**: every Mac running the app checks the same shared GitHub Releases feed, so cutting one release updates every install, not just whichever machine it was built on.
- **Releases**: cut a new version with: bump `version` in `package.json` → `npm run dist:mac` → `git add -A && git commit -m "..." && git push` → `gh release create vX.Y.Z "dist/Unified Integration CRM-X.Y.Z-arm64.dmg" "dist/Unified Integration CRM-X.Y.Z-x64.dmg" --title "vX.Y.Z" --notes "..."`. The repo (`crm/` only, not the marketing site) is public at `github.com/antwan36/unified-integration-crm`, git-initialized fresh in this session (not connected to the parent "Unified Integration" folder's repo).
- **Packaging**: `npm run dist:mac` (via `electron-builder`) → `.dmg` (both arm64 and x64) in `crm/dist/`. Unsigned (no Apple Developer cert) — first launch on a new Mac needs `xattr -cr` or right-click-Open once. On this iCloud-synced repo, `electron-builder`'s lockfile can occasionally throw "Unable to update lock within the stale threshold" if a build races another process (e.g. iCloud sync, or another session building concurrently) — just retry.

## Known issues (as of 2026-07-30)
- **RESOLVED — `estimate-portal` deployment**: the project's live alias is now `https://estimate-portal-one.vercel.app`, not the old `unified-integration-estimates.vercel.app` referenced in earlier versions of this doc (that domain/alias is simply gone — don't waste time investigating it, it's not coming back). Verified live 2026-07-30: `vercel ls` shows recent "Ready" production deployments, `/cal/:token` and `/e/:token` both respond correctly (an invalid test token correctly returns a 403 "Invalid calendar link" from the app itself, not an infra error), and the CRM's own Settings → "Quote signing page" is already correctly pointed at `estimate-portal-one.vercel.app` with a green "Connected" banner. **Clients can sign estimates and the calendar feed works.** If this ever needs a fresh deploy: `cd estimate-portal && npx vercel --prod` (Vercel CLI is authenticated as `emeraldman2000-1669` as of 2026-07-30 — check `npx vercel whoami` before assuming no access).
- **Stray Square customer keeps reappearing as a lead**: a "ZZTEST Verify Round2" contact repeatedly reappears in Contacts/Dashboard with `source: square` even after being deleted in the CRM. Root cause: earlier test-invoice verification created a *real* Square customer record via `ensureSquareCustomer()` as a side effect; deleting the local CRM contact only removes the local Postgres row, not the Square-side customer, so the two-way `runSquareSync()` (every 10 min + on-demand) keeps re-importing it. There is currently no "delete in Square too" action anywhere in the app (only invoices can be canceled/deleted in Square, not customers). Fix requires either deleting the customer directly in the Square Dashboard (Customers → find "ZZTEST Verify Round2" → delete), or building a real delete-in-Square action for contacts. Still unresolved as of 2026-07-30 — flagged to the user rather than guessed at.
- **Google review link not configured** — Review Requests can queue and be reviewed, but sending is blocked until Settings → "Google review link" is set (get it from business.google.com → your profile → "Get more reviews" → Copy link). Needs the user; not something to fabricate.
- **Plaid credentials not configured** — bank-account syncing is fully built (see Finances above) but inert until Settings → "Plaid (bank accounts)" has a real Client ID/Secret. Sandbox keys are free/instant from the Plaid dashboard; production requires requesting access from Plaid first. Needs the user's own Plaid developer account.
- Everything else — the CRM app itself, including the new Plaid/Payroll/Inventory/Review Requests/Form Leads features — passed a full typecheck, a clean `dist:mac` build, and was reinstalled + spot-verified live (real Neon data, real logged-in session) as of 2026-07-30.

## Known gotchas hit during development (don't redo this work)
- `better-sqlite3` and node-gyp **do not work** in this repo's path — the project directory has spaces in it (`Unified Integration`), which breaks native module compilation. The DB layer uses `pg` (pure JS) for this reason.
- Square's Create Invoice API requires `accepted_payment_methods` AND `delivery_method` in the request body even for a draft — both are set unconditionally in `createSquareInvoice()`.
- **Stale running processes**: if you rebuild and reinstall while an old instance is still running, macOS will just refocus the old process. Always fully quit (`pkill -f "Unified Integration CRM"`, confirm with `ps aux`) before relaunching after a reinstall.
- **Dev/packaged storage collision (fixed)**: dev (`npx electron .`) and packaged builds used to share the same `userData` folder, wiping the real app's workspace connection during dev testing. Fixed in `src/main/index.ts` via a separate `unified-integration-crm-dev` folder for unpackaged runs.
- **Settings text inputs with a placeholder that looks like real data**: several Settings fields use the actual production value as their `placeholder` (e.g. the estimate signing page input's placeholder literally is `https://unified-integration-estimates.vercel.app`). Gray placeholder text is easy to mistake for a saved value when spot-checking via screenshot — always confirm via the green "Connected: …" banner (or a console `window.api.settings.get…()` call), not the input's displayed text.
- **Computer-use click timing on this app**: clicks immediately after a page transition or on freshly-rendered buttons have occasionally not registered (Save buttons, nav links) — if a screenshot shows no state change after a click, retry the exact same click once before concluding something is broken.
- **Intermittent `AggregateError` from `pg`**: occasional IPC-triggered Postgres queries throw `AggregateError` (Neon connection blip) — most common right after a fresh launch (e.g. an empty/broken Cmd+K search on the very first open), but has also been observed later in a session on a single action (e.g. deleting an invoice failed twice in a row with `AggregateError` before succeeding on the third click). Retrying the same action resolves it. Not treated as a code bug — no fix attempted, since it's Neon-side connection churn, not application logic.

## Feature set
Leads/email ingestion (IMAP+Formspree) across any number of configured email accounts, SMTP send/reply (per-account "from" selection), a unified Email inbox across all contacts with read/unread tracking and compose-to-any-address, a raw Form Leads log of every Formspree submission, Square invoicing (send-now or save-as-draft, cancel/delete, partial payment + refund tracking, internal-only cost tracking for profit margin), custom-built e-signature Estimates (external Vercel portal at `estimate-portal-one.vercel.app`, working), Tasks with optional scheduling (`startAt`/`endAt`/`location`, feeds the calendar subscription) plus a dedicated Calendar month-grid page to view and add them, stale-lead follow-up panel, per-contact lifetime revenue + an invoices-page revenue chart, file attachments (stored as Postgres `bytea`, 10MB cap), contact job type + filter, desktop notifications (new lead / invoice paid), CSV export (contacts/invoices), team accounts, a reusable priced Catalog with product-link auto-scraping into line items, a global Cmd+K search palette, a subscribable calendar feed for scheduled jobs, in-app self-updates via GitHub Releases, and a Finances section covering Plaid bank-account sync, payroll/1099 tracking, and inventory — plus an auto-queued Review Requests flow triggered by paid invoices.

## Verification approach used throughout
Every feature has been verified against the **real** Neon database and **real** Square production account (not just typechecked) using computer-use to drive the actual running app, with test data cleaned up afterward. SMTP sending itself was never verified end-to-end — no test mailbox credentials were available.

## Current state
- App is installed at `/Applications/Unified Integration CRM.app`, currently **v1.0.3**, connected to the real Neon workspace and real Square production account, with live lead/invoice data flowing in normally. Both arm64 and x64 `.dmg`s for the latest version should be attached to the matching GitHub release at `github.com/antwan36/unified-integration-crm/releases` (confirm the v1.0.3 release exists — see Releases section for the cut command if not).
- **One email account is configured** as of 2026-07-30: `info@unifiedintegrationpa.com` via `imap.hostinger.com` (Settings → "Email accounts" shows it connected). Earlier notes saying no account was configured are stale.
- Square is connected in **production** mode (Settings shows "Connected: production — Unified Integration").
- Plaid and the Google review link are both still unconfigured — see Known issues.
- No "second Mac" has actually been set up yet — only this one Mac has the app installed.
- No automated tests exist; verification has been manual/live-app only, driven by Claude via computer-use.

## Credentials note
Real secrets (Square production token, Neon DB password) were shared in chat during setup at the user's initiative. They were never written to any file in this repo and were only ever typed directly into the running app's UI. If you're picking this up fresh, you won't find any secrets in this repo — check `.gitignore` and the Settings UI in-app instead.
